---
name: "API 文档生成"
description: "为 API 接口生成完整文档"
variables:
  - name: file_path
    label: "接口文件路径"
    placeholder: "如：include/api/client.h"
  - name: language
    label: "编程语言"
  - name: code
    label: "接口代码"
    multiline: true
  - name: doc_format
    label: "文档格式"
    placeholder: "如：Markdown、Doxygen、OpenAPI"
---

请为以下 API 生成文档：

**文件**：`{{file_path}}`
**格式**：{{doc_format}}

```{{language}}
{{code}}
```

---

## API 文档内容

### 必需内容
1. **概述** - API 的用途和功能
2. **接口定义** - 每个接口的签名
3. **参数说明** - 参数类型、含义、约束
4. **返回值** - 返回值说明
5. **错误处理** - 可能的错误和处理
6. **使用示例** - 典型用法

### 可选内容
- 版本信息
- 废弃说明
- 性能考虑
- 线程安全说明
- 相关接口

---

## 输出格式

### API 概述

简要描述这组 API 的功能和使用场景。

### 接口列表

| 接口 | 描述 | 参数 | 返回值 |
|-----|------|------|-------|
| ... | ... | ... | ... |

### 详细文档

#### `接口名`

**功能**：简要描述

**声明**：
```{{language}}
ReturnType functionName(params);
```

**参数**：

| 参数 | 类型 | 方向 | 描述 | 约束 |
|-----|------|------|------|------|
| param1 | Type | in | ... | 非空 |

**返回值**：

| 类型 | 描述 |
|-----|------|
| ... | ... |

**错误**：

| 错误类型 | 条件 | 处理建议 |
|---------|------|---------|
| ... | ... | ... |

**示例**：
```{{language}}
// 使用示例
```

**注意事项**：
- ...

---

## 安全边界

⚠️ **文档注意事项**：
- 文档应与代码同步更新
- 标注废弃的 API
- 说明线程安全性
- 说明性能影响
- 版本兼容性说明
